{
    "menu_name": "Anaconda${PY_VER} ${PLATFORM}",
    "menu_items":
    [
    {
        "name": "Bash (Git)",
        "script": "${PREFIX}/Library/git-bash-win/bin/bash.exe",
        "scriptarguments": ["--login", "-i", "--"],
        "workdir": "${PERSONALDIR}",
        "icon": "${MENU_DIR}/git-bash-win.ico"
    }
    ]
}
